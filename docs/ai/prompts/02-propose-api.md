# Prompt 2 — Design Symfony API Layer

Based on the previously analyzed ROLE_USER web flows, propose the minimal Symfony API layer required for the React Native add-on.

Do not modify files.
Do not generate code yet.

Provide:

1. Proposed API endpoints
2. HTTP methods
3. Route patterns
4. Purpose of each endpoint
5. JSON response shapes
6. Authentication requirements
7. Which endpoint establishes or returns the active league context
8. Which data should be cached in the mobile app

Constraints:

- The existing web application must remain unchanged.
- The API layer should be additive only.
- Reuse existing controller and entity logic whenever possible.
- Do not introduce new services unless clearly necessary.
- Keep the API surface minimal.

Return a clear API proposal.