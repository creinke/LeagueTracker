<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
	<div class="container">
		<a href="/" class="navbar-brand">League Tracker</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
			<span class="navbar-toggle-icon"</span>
		</button>
    	<div class="collapse navbar-collapse" id="mobile-nav">
    		<ul class="navbar-nav ml-auto">
    			<li class="nav-item">
    				<a href="{{ path('home') }}" class="nav-link">Home</a>
    			</li>
                {% if is_granted('ROLE_SUPER') %}
                    <li class="nav-item">
                        <a href="{{ path('course_list') }}" class="nav-link">Courses</a>
                    </li>
                {% endif %}
                {% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
                </li>
    			{% else %}
                <li class="nav-item">
                    <a href="{{ path('app_login') }}" class="nav-link">Login</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
        			<li class="nav-item">
        				<a href="{{ path('player_list') }}" class="nav-link">Players</a>
        			</li>
                    <li class="nav-item">
                        <a href="{{ path('season_list') }}" class="nav-link">Seasons</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('team_list') }}" class="nav-link">Teams</a>
                    </li>
                {% endif %}
    		</ul>
    	</div>
	</div>
</nav>
