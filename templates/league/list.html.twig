{# templates/league/list.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <style>
        .btn {
            padding: 0.15rem 0.275rem;
        }
        .table td {
            padding: 0px;
            height: 1.5em;
        }
        .table th {
            padding: 0px;
            height: 1.5em;
        }
    </style>

    {% if not leagues %}
        <div class="text-right"> <!--You can add col-lg-12 if you want -->
            <a href="{{ path('league_new') }}" class="btn btn-dark">Add New League</a>
       </div>
       
       <p>No leagues yet defined</p>
    {% else %}
        <h2>Leagues</h2>
    
        <div class="text-right"> <!--You can add col-lg-12 if you want -->
            <a href="{{ path('league_new') }}" class="btn btn-dark">Add New League</a>
       </div>

        <div class="table-responsive"> 
            <table class="table table-striped table-condensed">
                <thead>
                    <tr>
                        <th>League</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for league in leagues %}
                        <tr>
                            <td>{{ league.name }}</td>
                            <td>
                                <a href="{{ path('league_view', {'id': league.id}) }}" class="btn btn-info btn-sm" data-method="GET">View</a>
                                <a href="{{ path('league_delete', {'id': league.id}) }}" class="btn btn-danger btn-sm" data-method="DELETE">Delete</a>
                                <a href="{{ path('league_edit', {'id': league.id}) }}" class="btn btn-primary btn-sm" data-method="POST">Edit</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
{% endblock %}